# Runtime evolution state lives OUTSIDE the repo (default: ~/.claude/smartthink-vault/,
# override with $SMARTTHINK_VAULT). skill/.data/evolution-state.md is only the empty
# template that install.sh seeds into the vault - keep it blank.

# Task-runner state (fablize)
.fablize/
