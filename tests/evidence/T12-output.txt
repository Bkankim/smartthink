# T12 헤드리스 표준 출력·표준 오류

실행일: 2026-09-08 02:22~02:49 (+09:00) | 임시 vault: $ST_VAULT (=SMARTTHINK_VAULT, T1~T6와 동일) | 부모 Claude Code 세션 안에서 중첩 실행(CLAUDECODE=1)

판정: **PASS** - run2에서 게이트 자동 진행, `--budget 200000` 대신 헤드리스 상한 120K 적용(manifest.budget = "120000"), 절삭 내역(리서치 축약→OFF, 메타인지 제외)과 이유 출력, 팩 경로·1절 브리핑·"무장 완료" 안내 후 턴 종료(본 작업 미착수). `check-structure.py --pack` H. 5항목 PASS(전체 exit 1은 tests/evidence 복사본 em dash 위생 검사 때문).

관찰(판정 외):
- run1(문서 원문 명령 그대로)은 `Not logged in · Please run /login` exit 1. 부모 세션의 `ANTHROPIC_OAUTH_TOKEN`은 자식 `claude`가 읽지 않고 키체인도 중첩 자식에서 미해석. `claude auth status` 탐침(4종) 결과 `CLAUDE_CODE_OAUTH_TOKEN` 주입 시에만 loggedIn=true. run2는 그 값을 주입해 실행(토큰은 증거에 미기록).
- 헤드리스 세션도 `/st`가 전역 v2를 먼저 열었고, v2가 `--budget`을 지원하지 않는다는 이유로 스스로 `smartthink:smartthink`(v3)를 재호출했다(출력 0항목 "호출 경로" 줄). 사용자 개입 없는 자가 교정이며, 다른 세션에서는 v2 경로로 그대로 진행될 수 있다.
- 헤드리스 자식이 부모와 같은 사용자 CLAUDE.md 규칙을 상속해 `tests/evidence/T12-manifest.json`·`T12-pack.md`를 스스로 복사했고(바이트 동일 확인), 프로젝트 메모리(`~/.claude/projects/.../memory/smartthink-legacy-skill-shadows-v3-plugin.md`)와 Hindsight에 gotcha를 기록했다고 보고했다. 리포의 다른 파일은 건드리지 않았다(`git status` 확인).
- 절삭 순서 관찰은 T2와 동일(리서치를 먼저 끈 뒤 모듈 제외로 여유가 생겨도 복원 규칙 없음).

## run1 (문서 원문 명령 그대로)
```
T12 run1 start: 2026-09-08T02:23:09+0900
cmd (문서 원문): SMARTTHINK_VAULT="$ST_VAULT" claude --plugin-dir . -p '/st --budget 200000 지역 도서관 예약 시스템의 장기 개선 전략을 검토해줘'
env: CLAUDECODE=1 (부모 Claude Code 세션 안에서 중첩 실행)
---- stdout+stderr ----
Not logged in · Please run /login
exit=1 end: 2026-09-08T02:23:10+0900
```

## run2 (자식 프로세스용 OAuth 토큰 주입)
```
T12 run2 start: 2026-09-08T02:26:05+0900
cmd: CLAUDE_CODE_OAUTH_TOKEN=[REDACTED] SMARTTHINK_VAULT="$ST_VAULT" claude --plugin-dir . -p '/st --budget 200000 지역 도서관 예약 시스템의 장기 개선 전략을 검토해줘'
사유: run1이 'Not logged in'으로 exit 1. 부모 세션 env의 ANTHROPIC_OAUTH_TOKEN은 자식 claude가 읽지 않고, 키체인도 중첩 자식에서 미해석. claude auth status 탐침 결과 CLAUDE_CODE_OAUTH_TOKEN 주입 시에만 loggedIn=true(oauth_token).
---- stdout+stderr ----
무장 완료. 헤드리스 실행이라 게이트를 자동 진행했고, 전달된 `--budget 200000` 대신 헤드리스 상한 120K를 적용해 절삭했다. 아래는 게이트 출력, armorer가 만든 팩의 1절 브리핑 원문, 팩 요약 순서다.

```
━━ SmartThink 무장 게이트 ━━   (헤드리스: 자동 진행, 상한 120K)

0. 능력 감지
   Agent 도구 있음 → armorer 경로 / WebSearch·WebFetch·insane-search 있음
   SKILL_DIR = <repo>/skills/smartthink / VAULT = $SMARTTHINK_VAULT (profile.md 선언값과 동일, 쓰기 가능)
   비대화식(sdk-cli 진입점) → 게이트 자동 진행, 상한 120K
   호출 경로: /st → ~/.claude/commands/st.md(사용자 레벨) → ~/.claude/skills/smartthink(v2, 7/22자)가 먼저 로딩됨.
             v2는 --budget 미지원이라 플러그인 v3(smartthink:smartthink)로 재호출. armorer는 smartthink:st-armorer로 스폰.

1. 입력 해석
   지역 공공 도서관 예약 시스템(좌석·자료·프로그램·공간)의 3~5년 개선 전략 검토. 기존 전략 문서가 없으므로
   "검토"는 현행 진단 → 장기 방향 후보 → 우선순위·로드맵 → 리스크·판단 기준을 수립·평가하는 작업으로 해석.
   산출물은 전략 검토 의견이며 화면 설계·구현이 아님. 관 수·예산·벤더 계약·이용 로그·광역 연계는 미확정.

2. 진단
   Cynefin: Complicated (Complex 경계, 자동 판정 Complicated)  |  분류: 전략 수립

3. 추천 모듈 (5개, ★ = 주력)
   ★ 핵심 엔진 - 제1원리 분해(예약이 다루는 실제 희소성 재정의), 역전 사고(실패 경로 역추적), 복합 우위 적층(데이터→신뢰→이용률 플라이휠 순서)
   ★ 인지 무기고 - 이용자 행동·조직/제도 모델·게임이론을 전략 옵션 평가에 적용
   ★ 안티프래질 전략 - 바벨(안정 운영 + 소규모 실험), 옵셔널리티(벤더 교체 옵션), 볼록성으로 하방 방어
     실행 속도 - OODA 루프로 분기 단위 실행·측정, 가역/비가역 결정 구분
     메타인지 - Wardley 맵으로 구성요소 진화 단계 판정 (→ 절삭으로 제외)

4. 예상 비용 (추정치, index.json 사전 계산값)
   적용 상한: 120K - 헤드리스 상한. 전달된 --budget 200000은 120K를 넘으므로 우선하지 않고 120K로 하향.
   [절삭 전] 팩 ≈ 137K (원문 5개 129.8K + 적용 레이어 2.0K + 1·2·6절 2K + 3절 3K) / 서브에이전트 ≈ 150K → 초과
   절삭 적용(순서대로):
     1) 리서치 축약(3K→1K) → ≈134.8K 초과 → 리서치 OFF(3절 생략, manifest.research=false) → ≈133.8K 초과
     2) ★ 없는 5순위 메타인지(24.7K) 제외 → ≈108.7K ≤ 120K, 절삭 중단. 4순위 실행 속도 유지
     3) 적용 레이어 축약 - 불필요
   메인에 실릴 팩      ≈ 109K 토큰 (105.1K 원문 4개 + 1.6K 적용 레이어 + 2K 1·2·6절)
   서브에이전트 작업    ≈ 110K 토큰 (105.1K 원문 읽기 + 5K 쓰기·정리, 리서치 0)
   ※ 두 값 모두 추정이며 실제와 다를 수 있습니다.

5. 리서치: OFF (헤드리스 120K 절삭 1단계로 꺼짐 - 사용자 선택 아님)

6. 조작: 헤드리스 - Enter 대기 없이 자동 진행. 모듈 ★3 + 보완 1(절삭 후) 추천 그대로 확정.
```

armorer 반환: 팩 생성 완료. 1·2·4·5·6절 있음, 3절 생략. 모듈 4개 원문 복사, 리서치 미수행.

## 1. 무장 브리핑

**활성 프레임 3**

1. **제1원리 분해 (핵심 엔진 #1)** - "예약 시스템은 이래야 한다"는 가정 20개를 먼저 벗겨내고, 좌석·자료·공간의 물리적 유한성과 법·제도 제약만 남긴 뒤 장기 방향 후보 3개를 백지에서 재조합하는 데 쓴다. 현행 진단과 방향 후보 생성 단계의 주 엔진이다.
2. **역전 사고 (핵심 엔진 #11 + 인지 무기고 #53)** - 3~5년 로드맵이 "반드시 실패하려면" 무엇이 필요한지 실패 모드 10개 이상을 카탈로그로 만들고, 확률×영향 상위 5개에 제거·완화·대비 계획을 붙인다. 리스크와 재검토 시점 항목의 주 엔진이다.
3. **바벨 + 옵셔널리티 (안티프래질 전략 2·3장)** - 예산·시간·시스템 변경을 90% 안정 운영과 10% 파일럿 실험으로 갈라 "약간 개선된 리뉴얼" 중간지대를 거부하고, 벤더·표준 선택처럼 비가역적인 결정은 옵션 가치가 남는 쪽으로 미룬다. 우선순위와 단계 배열의 판단 기준이다.

보조로 복합 우위 적층(#9)이 단계 순서(무엇을 먼저 쌓아야 다음이 복합되는가)를, 실행 속도가 결정 유형별 속도(Type 1/Type 2)를 담당한다.

**이 작업에 적용할 규칙**

- 결론을 쓰기 전에 가정 목록 20개와 실패 모드 10개를 먼저 써라. 둘 중 하나라도 비어 있으면 그 결론은 아직 근거가 없다.
- 미확정 5항목(관 수, 예산, 벤더 계약, 이용 로그, 광역 연계)은 각각 "이 값이 A면 권고가 X, B면 Y"로 분기해 써라. 하나의 값을 가정하고 단일 권고를 내지 마라.
- 장기 방향 후보는 안내 화면 층 1개 이상 + 규칙·데이터·시스템 구조 층 1개 이상을 반드시 포함하라. 화면 개선만으로 후보 3개를 채우지 마라.
- 모든 권고를 Type 1(비가역: 벤더 교체, 광역 표준 채택, 데이터 모델 확정)과 Type 2(가역: 예약 규칙 튜닝, 안내 문구, 알림 채널)로 분류하고, Type 1에는 프리모텀과 옵션 유지 조건을, Type 2에는 파일럿 킬 기준을 붙여라.
- 로드맵의 각 단계에 졸업 기준(파일럿 → 전관 확대 조건)과 퇴출 기준을 수치로 적어라. 수치를 댈 로그가 없으면 "로그 확보"가 그 단계보다 앞선 단계다.
- 멘탈 모델을 나열하지 마라. 모델 하나를 꺼내면 그 모델의 적용 질문을 이 작업의 결정 하나에 붙이고 답을 써라.
- 노쇼 패널티, 예약 상한 같은 규칙 변경은 2차·3차 효과("그 다음에는?")를 3회 반복해 쓴 뒤에만 권고하라.
- 팩 밖의 사실 주장(국내 벤더 현황, 이용률 수치, 타 지자체 사례)에는 [미확인] 표시를 붙여라. 이 팩에는 리서치 합성이 없다.
- 현행 진단은 이용자 측 고통점과 직원 측 고통점을 별도 열로 써라. 한쪽만 쓴 진단은 반쪽이다.
- 방향 후보마다 트랙 1(단계 수·대기 시간·처리량 같은 수치)과 트랙 2(부서 인센티브·벤더 이해관계·현장 저항)를 따로 평가하라. 두 트랙이 어긋나면 우선순위를 보류하고 이유를 적어라.
- 재검토 시점은 날짜와 조건 둘 다로 써라. 조건 중 최소 하나는 그레이 코뿔소(벤더 계약 만료, 광역 통합 일정, 시스템 노후)에 걸어라.

**관련 과거 인사이트**

없음 (진화 상태 v3 빈 시드: 세션 0, 인사이트 0/10, 갭 0/5)

**경계할 편향**
1. **리서치 부재의 확신 (지식 결핍 유래)** - 3절이 없어 국내 공공 도서관 예약 시스템의 벤더 구조·광역 통합 서비스 연계·노쇼율 같은 사실을 기억에서 끌어와 단정할 위험이 크고, 이 작업의 전략 권고는 그 사실에 직접 의존한다.
   회피: 결론 전에 "확인 필요 사실 목록"을 별도 블록으로 먼저 쓰고, 본문에서 그 목록 밖의 도메인 사실을 쓸 때마다 [미확인]을 붙인다.
2. **안내 흐름 조기 수렴 (작업 해석 + 프로필 유래)** - 프로필의 현재 목표(예약 대기열·수업 신청·방문 동선 안내 흐름 개선, 2026 Q4)가 3~5년 전략을 "안내 화면 개선 로드맵"으로 좁힐 위험이 있다. 장기 전략의 큰 지렛대는 규칙·데이터·시스템 구조 층에 있을 수 있다.
   회피: 방향 후보 3개를 쓸 때 안내 흐름이 아닌 층의 후보를 첫 번째로 쓴다.
3. **안티프래질 과잉 헤지 (선택 모듈 유래)** - 옵셔널리티와 바벨을 적용하면 모든 방향을 "옵션으로 유지"로 미뤄 우선순위 없는 전략 검토가 나올 수 있다. 검토 산출물은 권고와 우선순위가 핵심이다.
   회피: 로드맵 1단계는 옵션이 아닌 확정 행동 1개로 쓰고, 방향 후보마다 바벨 판정(안전/실험/중간지대 거부) 결과를 한 줄로 명시한다.

**다음 단계**

무장이 끝났다. 이제 본 작업(현행 진단 → 장기 방향 후보 → 우선순위·단계별 로드맵 → 리스크·판단 기준·재검토 시점)에 착수하면 된다.
이 팩의 4절 적용 레이어가 작업 순서를, 5절 원문이 각 프레임의 절차를 제공한다. 깊이 우선 스타일에 맞춰 `/effort xhigh`를 권장한다.
리서치 없음(헤드리스 120K 상한 절삭, 사용자 선택 아님): 도메인 수치·플레이어·반대 신호는 이 팩에 없다. 필요하면 본 작업에서 별도 검색하거나 사용자에게 현행 시스템 정보를 요청하라.
기존 전략 문서가 나중에 제공되면 이 팩은 그대로 두고 작업을 "그 문서의 평가"로 전환한다.

```
팩: $SMARTTHINK_VAULT/packs/2026-09-08-library-reservation-longterm-strategy/pack.md  |  모듈: 핵심 엔진, 인지 무기고, 안티프래질 전략, 실행 속도  |  비용: 팩 ≈118K / 작업 ≈116K  |  리서치: OFF
※ 게이트 추정(팩 ≈109K / 작업 ≈110K)과 실제(팩 ≈118K / 작업 ≈116K)의 차이 약 9K는 1·2·4절이 추정 상수보다 커서 생겼고, 120K 상한 안이다.

무장 완료. 이제 작업을 지시하세요.
깊은 작업이라면 /effort xhigh 를 먼저 실행하는 것을 권장합니다.
```

**게이트 실행자 메모 (T12, 판정 참고용)**

- **호출 이름 가림 현상.** 이 머신의 사용자 레벨 `~/.claude/skills/smartthink`(v2)와 `~/.claude/commands/st.md`가 `--plugin-dir` 플러그인의 `/st`·`smartthink` bare 이름을 가린다. v3는 `smartthink:smartthink`, `smartthink:st-armorer` 네임스페이스로만 도달한다. 다음 헤드리스 게이트 전에 레거시 스킬·커맨드를 치우거나 이름을 바꾸는 것을 권한다. 이 gotcha는 Hindsight와 프로젝트 메모리에 기록했다.
- **검증 결과.** `python3 scripts/check-structure.py --pack <팩 디렉터리>`에서 H.pack 5항목(파일 존재, manifest 스키마와 boolean research, 절 제목 순서, 5절 해시 무결성, 마커 범위) 전부 PASS. 전체 종료 코드는 1인데, 원인은 T2에서 이미 기록된 G.hygiene 검사가 `tests/evidence/*.md`에 복사된 레퍼런스 원문의 em dash를 잡기 때문이며 팩 계약 위반이 아니다.
- **증거 파일.** `tests/evidence/T12-manifest.json`, `tests/evidence/T12-pack.md`를 원본 그대로 복사했다(바이트 동일, 임시 vault 경로 포함 0건). 표준 출력·오류(`T12-output.txt`)는 이 헤드리스 호출을 실행한 셸에서 캡처해야 한다.
- **절삭 순서 관찰(T2와 동일한 설계 갭).** 순서 1에서 리서치를 먼저 끈 뒤 순서 2에서 메타인지를 빼자 여유가 생겼지만, 앞 단계 절삭을 되돌리는 규칙이 없어 리서치는 OFF로 남았다. 메타인지만 뺐다면 리서치 ON으로도 약 112K로 상한 안에 들어온다. 통과 기준 항목은 아니라서 규칙대로 진행했다.
- **본 작업(전략 검토)에는 착수하지 않았다.** 사용자 지시를 기다린다.
exit=0 end: 2026-09-08T02:48:57+0900
```
